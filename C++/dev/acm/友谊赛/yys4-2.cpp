#include<iostream>
#include<stdlib.h>
using namespace std;
void J(long long *num,long long k,long long n){
	for(int i = 1;i <= n;i++)
}
int main(){
	long long n, k;
	while (cin >> n >> k) {
		long long *num = (long long *)malloc((n+1) * sizeof(long long));
		cout << J(L, k) << endl;
	}
	return 0;
}
